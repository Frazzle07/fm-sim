import type { ZoneConfig } from "./types";

// SS (second striker) sits slightly left of centre and a touch deeper than CF.
export const SS: ZoneConfig = {
	xMin: 0.2,
	xMax: 0.8,
	yMinDeep: 0.41,
	yMinHigh: 0.45,
	yMaxDeep: 0.6,
	yMaxHigh: 0.95,
	idealBallOffset: 0.12,
};
